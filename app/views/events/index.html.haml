#timeline



:javascript
  var data = #{@json};
  
  var timeline_config = {
      type:       'timeline',
      width:      '100%',
      height:     '100%',
      source:     data,
      embed_id:   'timeline',
      debug:              true
  }

  $(document).ready(function() {
                createStoryJS(timeline_config);
            });